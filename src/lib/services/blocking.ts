// Blocking service integration
import { blockManager } from '@/lib/managers/BlockManager';
}
  }
    };ntBlocks: 0,ermane0,
      plocks:     totalB,
  Remaining: 0sessionTimee,
      ion: falsessveS    hasActiturn {
  
    re, error);atus:' blocking stngettior('Error gsole.err{
    con(error) } catch 
    };
  nentBlocks,rmaockStats.peks: blocpermanentBl    ks,
  BlocotalblockStats.talBlocks:    tot
   : 0,w()) Date.nondTime - ssion.eSeiveh.max(0, acton ? Matsig: activeSesTimeRemainin   sessiontive),
   .isAceSessionactivsion && tiveSesacn: !!(tiveSessio hasAc    {
  rn  retu
      );
ts(ckStaloer.getBlockManagts = bst blockSta
    con();onveSessiger.getActiSessionMana await focusn =iveSessioconst act
    ry {
  ttus() {tBlockingStaunction geort async f}

exp
lse;
  } return fa
   error);:', L blocking URror checkingrror('Er  console.e  (error) {
catch alse;
  } return f   
  }
    }
   rue;
  n t   retur     id);
ockItem.emAccess(bl.updateItManager block await       ) {
eakOnBr    if (!is    
  me;
  dTi.eneak !lastBrstBreak &&Break = laOn    const is;
  length - 1]n.breaks.veSessioreaks[actission.b= activeSe lastBreak stcon     on break
 ck if  Che
      //ive) {sActon.i activeSessiion && (activeSess    if
();iontiveSessAcnManager.getiocusSessawait foion = activeSessnst cos
    tive sessionly during ac onemsit temporary ck  // Blo  
    }

eturn true;d);
      rtem.icess(blockIeItemAcdat.upManagerock   await bl) {
   sPermanent(blockItem.ims
    if ent iteock permanys bl    // Alwa;

urn false retlockItem)f (!b
    irl);em(uckedItgetBloer.anagckM = await blotemt blockIm
    consy block itees anif URL match // Check   try {
   boolean> {
: Promise<ring)st: ked(urlon isUrlBlocncti fuxport asyncger';

eSessionManaers/Focus'@/lib/managm ro fanager }sSessionM { focumport
i